extends layouts/main.pug

block content   
    .form-container
        form(action=routes.editVideo(video.id), method="post")
            input(type="text", placehlolder="Title", name="title", value=video.title)
            textarea(placehlolder="Description", name="description")=video.description
            input(type="submit", value="Update Video")
        a.form-container__link(href=routes.deleteVideo(video.id))
            button.delete Delete Video 